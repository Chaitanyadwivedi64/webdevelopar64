       
        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
        }

        body {
            font-family: system-ui, -apple-system, sans-serif;
            color: #ffffff;
            background-color: #001f3f;
            overflow-x: hidden;
            min-height: 100vh;
            padding-top: 80px; 
        }

        /* --- School Navigation Bar --- */
        .navbar {
            position: fixed;
            top: 0; left: 0; right: 0;
            height: 70px;
            background: rgba(15, 15, 15, 0.75);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 2rem;
        }

        .nav-logo {
            font-size: 1.4rem;
            font-weight: 700;
            color: #ffffff;
            text-decoration: none;
            letter-spacing: 1px;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
        }

        .nav-item a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .nav-item a:hover {
            color: #FCB216;
        }

        .nav-cta {
            background-color: #FCB216;
            color: #000000 !important;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            font-weight: 600 !important;
            transition: transform 0.2s ease, background-color 0.3s ease !important;
        }

        .nav-cta:hover {
            background-color: #e59d0b !important;
            transform: translateY(-1px);
        }

        /* --- School Main Banner Image Style --- */
        .school-banner-container {

            max-width: 600px;
            margin: 2rem auto 1rem auto;
            border-radius: 9999999999999999999999px;
            overflow: hidden;
            box-shadow: 0 8px 24px rgba(0,0,0,0.4);
            border: 10px solid rgba(255, 255, 255, 0.15);
        }

        .school-banner-img {

            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
        }
.img{background: linear-gradient(90deg, #FCB216 0%, #E85D24 35%, #D91B57 70%, #63205F 100%);}
        /* --- Moving Dots Canvas Background --- */
        #particle-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            z-index: 1;
            pointer-events: none;
        }

        /* --- Main Content Layout --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
            position: relative;
            z-index: 2;
        }

        .hero {
            text-align: center;
            background: rgba(15, 15, 15, 0.75);
            padding: 4rem 2rem;
            border-radius: 24px;
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: shadow-pop-tr 0.3s cubic-bezier(0.470, 0.000, 0.745, 0.715) both;
        }

        h1 { 
            font-size: 3rem; 
            margin-bottom: 0.5rem; 
        }

        .main-btn { 
            display: inline-block;
            margin-top: 1.5rem;
            border: 2px solid #FCB216; 
            color: #FCB216; 
            padding: 0.5rem 1rem; 
            text-decoration: none; 
            border-radius: 8px; 
            transition: all 0.3s ease;
            font-weight: 600;
        }
        .main-btn:hover {
            background-color: #FCB216;
            color: #000;
        }